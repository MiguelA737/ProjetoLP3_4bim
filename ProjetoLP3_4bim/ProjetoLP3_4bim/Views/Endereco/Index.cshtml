@model IEnumerable<ProjetoLP3_4bim.Models.Endereco>

@{
    ViewData["Title"] = "Lista";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lista</h2>

<p>
    <a asp-action="Create">Cadastrar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeroEndereco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RuaIdRuaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroEndereco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RuaIdRuaNavigation.NomeRua)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEndereco">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdEndereco">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEndereco">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
