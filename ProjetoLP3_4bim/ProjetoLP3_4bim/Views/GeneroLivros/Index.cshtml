@model IEnumerable<ProjetoLP3_4bim.Models.GeneroLivro>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeGeneroLivro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DesGeneroLivro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeGeneroLivro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DesGeneroLivro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdGeneroLivro">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdGeneroLivro">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdGeneroLivro">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
